import { PagePlaceholder } from "@/components/PagePlaceholder";

export function CouponsPage() {
  return (
    <PagePlaceholder
      title="Coupons"
      description="Discount codes and rules land with the coupons module (Phase 11)."
    />
  );
}
