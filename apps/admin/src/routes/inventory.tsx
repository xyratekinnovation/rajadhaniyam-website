import { PagePlaceholder } from "@/components/PagePlaceholder";

export function InventoryPage() {
  return (
    <PagePlaceholder
      title="Inventory"
      description="Stock levels per product variant land with the inventory module (Phase 10)."
    />
  );
}
