import { PagePlaceholder } from "@/components/PagePlaceholder";

export function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Store settings, admin users and role permissions land alongside admin authentication (Phase 5)."
    />
  );
}
