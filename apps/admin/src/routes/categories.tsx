import { PagePlaceholder } from "@/components/PagePlaceholder";

export function CategoriesPage() {
  return (
    <PagePlaceholder
      title="Categories"
      description="Category management (create, edit, reorder) lands alongside the products API in Phase 3."
    />
  );
}
