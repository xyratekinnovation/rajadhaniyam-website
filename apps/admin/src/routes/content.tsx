import { PagePlaceholder } from "@/components/PagePlaceholder";

export function ContentPage() {
  return (
    <PagePlaceholder
      title="Website Content"
      description="Editable homepage sections (story, testimonials, promises) land with the CMS/content module (Phase 12)."
    />
  );
}
