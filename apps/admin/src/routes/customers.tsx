import { PagePlaceholder } from "@/components/PagePlaceholder";

export function CustomersPage() {
  return (
    <PagePlaceholder
      title="Customers"
      description="Customer accounts, addresses and order history land with the authentication phase (Phase 5)."
    />
  );
}
