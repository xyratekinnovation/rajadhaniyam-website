{
  "name": "rajadhaniyam",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run dev:storefront",
    "dev:storefront": "bun run --cwd=apps/storefront dev",
    "dev:admin": "bun run --cwd=apps/admin dev",
    "dev:api": "bun run --cwd=apps/api dev",
    "build": "bun run build:storefront && bun run build:admin && bun run build:api",
    "build:storefront": "bun run --cwd=apps/storefront build",
    "build:admin": "bun run --cwd=apps/admin build",
    "build:api": "bun run --cwd=apps/api build",
    "lint": "bun run --cwd=apps/storefront lint && bun run --cwd=apps/admin lint",
    "typecheck": "bun run --cwd=apps/storefront typecheck && bun run --cwd=apps/admin typecheck && bun run --cwd=apps/api typecheck && bun run --cwd=packages/shared typecheck && bun run --cwd=packages/database typecheck && bun run --cwd=packages/ui typecheck",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.7.3",
    "typescript": "^5.8.3"
  }
}
